# The anchor for every paper: the MLflow run id of the accepted reproduce
# run, plus its score. Proposals are measured as a delta against this run.
# Update only when a new reproduce is accepted as the official baseline.
#
# run_id values below are from the Docker reproduce runs on the local MLflow
# server; on the shared team server they will be the team's accepted runs.
distilbert-sst2:
  reproduce_run_id: "63a66c05ea3c47e9bfbce269ae5422b0"
  primary_metric: "accuracy"
  score: 0.9106
  accepted_by: "poc"

distilbert-emotion:
  reproduce_run_id: "36477e90d5ca40babc3e1bac4be8b592"
  primary_metric: "f1_macro"
  score: 0.9065
  accepted_by: "poc"
